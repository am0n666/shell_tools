# Aliases and such. Independent from the
# used shell (zsh, bash, ...)

alias s="sudo su"
alias t="cd huuuge_casino_tool"
alias hd="cd /data/data/com.termux/files/home/huuuge_casino_tool"
alias dc="dpkg --configure -a"
alias auu="apt update && apt -y upgrade"
alias ai="apt -y install"
alias c="clear"
alias q="exit"
alias al="apt list"
alias aes="apt edit-sources"
alias ar="apt -y remove \"$@\" && apt -y purge \"$@\" && 
apt -y autoremove"


# enable color support of ls and also add handy aliases
if [ -x /system/bin/dircolors ]; then
  test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
  alias ls='ls -a --color=auto'
  alias dir='dir --color=auto'
  alias vdir='vdir --color=auto'

  alias grep='grep --color=auto'
  alias fgrep='fgrep --color=auto'
  alias egrep='egrep --color=auto'
fi

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ls='ls -CFa'

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'

#alias android-connect="mtpfs -o allow_other /media/GalaxyNexus"
#alias android-disconnect="fusermount -u /media/GalaxyNexus"

alias android-connect="adb forward tcp:8888 tcp:8888 && mount '/data/$HOME/Arbeitsfläche/GalaxyNexus'"
alias android-disconnect="fusermount -u /media/GalaxyNexus"

# EOF
alias rp1="cd ~/huuuge_casino_tool && hcgu restore_dialog profiles"
alias rp2="cd ~/huuuge_casino_tool && hcgu restore_dialog _profiles"
alias rp3="cd ~/huuuge_casino_tool && hcgu restore_dialog _chips_"
alias rp4="cd ~/huuuge_casino_tool && hcgu restore_dialog diams"
alias hmm="cd ~/huuuge_casino_tool && hcgu main_menu"
alias gt="cd ~/huuuge_casino_tool && sudo ./xyz hc_gifts.txt"
alias clp="cd ~/huuuge_casino_tool && hcgu clean_profile"
